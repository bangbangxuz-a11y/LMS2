{
    "name": "CodePlayground Pro",
    "postStartCommand": "bash -lc 'CODEPLAYGROUND_OPEN_BROWSER=0 ./.vscode/start-backend.sh > /tmp/codeplayground-backend.log 2>&1 &'",
    "forwardPorts": [8001, 8000],
    "portsAttributes": {
        "8001": {
            "label": "CodePlayground Editor",
            "onAutoForward": "openBrowser",
            "protocol": "http"
        },
        "8000": {
            "label": "Python Project Server",
            "onAutoForward": "openBrowser",
            "protocol": "http"
        }
    },
    "customizations": {
        "vscode": {
            "settings": {
                "task.allowAutomaticTasks": "on",
                "terminal.integrated.defaultProfile.linux": "bash",
                "terminal.integrated.automationProfile.linux": {
                    "path": "/usr/bin/bash"
                }
            }
        }
    }
}
